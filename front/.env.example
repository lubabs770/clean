# Copy this file to `.env` or `.env.local` and edit as needed
# During development the default, `/api`, works with the dev-server proxy.
# In production you may point to a deployed server URL.

VITE_API_BASE_URL=http://localhost:3001/api
